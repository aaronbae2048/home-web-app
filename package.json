{
  "name": "home-web-app",
  "version": "1.0.0",
  "description": "Frontend app for aaronbae.com.",
  "scripts": {
    "start": "node esbuild.config.mjs --start",
    "build": "node esbuild.config.mjs --build",
    "clean": "node esbuild.config.mjs --clean",
    "build:something": "esbuild ./src/app.jsx --bundle --outfile=./target/out.js"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "code-inspector-plugin": "0.17.7",
    "esbuild": "0.24.0",
    "esbuild-plugin-sass": "1.0.1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
